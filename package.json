{
  "name": "zksnarks-noir",
  "scripts": {
    "build": "npx hardhat clean && npx hardhat compile",
    "test": "npx hardhat test",
    "test:coverage": "npx hardhat coverage",
    "test:debug": "npx hardhat test --show-stack-traces",
    "deploy:localhost": "npx hardhat run --network localhost scripts/deploy.ts",
    "localhost": "npx hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.13.0"
  }
}
